import { HpDisplayPipe } from './hp-display.pipe';

describe('HpDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new HpDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
