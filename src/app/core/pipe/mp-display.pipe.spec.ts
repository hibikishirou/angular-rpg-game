import { MpDisplayPipe } from './mp-display.pipe';

describe('MpDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new MpDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
