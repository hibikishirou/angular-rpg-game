import { StaminaDisplayPipe } from './stamina-display.pipe';

describe('StaminaDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new StaminaDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
